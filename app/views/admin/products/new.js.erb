$(".modal").remove();
$("section.content").append(
  "<%= j render "admin/products/product_modal", object: @product, title: t("title.add_product"), submit: t("button.add") %>");
$(".modal").modal("show");
